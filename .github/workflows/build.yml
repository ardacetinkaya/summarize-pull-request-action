# This workflow will build a .NET project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-net

name: .NET Build

on:
  push:
    branches: [ "main" ]
    paths:
      - 'src/**'
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 8.0.x
    - name: Restore dependencies
      run: dotnet restore src/PR.Action/PR.Action.csproj
    - name: Build
      run: dotnet build src/PR.Action/PR.Action.csproj --no-restore
    # - name: PR Action
    #   id: dotnet-pull-request-action
    #   uses: ardacetinkaya/pull-request-action/@main
    #   env:
    #     PAT: ${{ secrets.ACTION_TOKEN }}
    #     APIKey: ${{ secrets.AI_API_KEY }}
    #     ModelId: gpt-4o
    #     URI: https://models.inference.ai.azure.com

